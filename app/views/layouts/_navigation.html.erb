<%= link_to "Strona główna", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
      <li>
        <%= link_to('Wyloguj się', destroy_user_session_path, :method=>'delete') %>
      </li>
  <% else %>
      <li>
        <%= link_to('Zaloguj się', new_user_session_path)  %>
      </li>
  <% end %>
  <% if user_signed_in? %>
  <% else %>
      <li>
        <%= link_to('Zarejestruj się', new_user_registration_path)  %>
      </li>
  <% end %>
</ul>