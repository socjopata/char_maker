%legend Wypełnij poniższy formularz:
= simple_form_for @message, html: {class: 'form-horizontal'} do |f|
  = f.error_notification
  = display_base_errors @message
  = f.input :email, label: "Twój email", value: current_user.try(:email)
  = f.input :name, label: "Twoje imię"
  = f.input :content, label: "Twoja wiadomość", as: :text
  %span.help-block.offset2= show_simple_captcha(label: "") unless current_user
.row
  %span.offset1= link_to t('common.send'), "#", class: "btn btn-primary", onclick: "$('form').submit()"
  %span.offset2= link_to t('common.return_to_main_page'), root_path, class: "btn"
